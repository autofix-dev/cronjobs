version: 2.0

jobs:
  autofix-workspace-images:
    docker:
      - image: gitpod/workspace-full:latest
    steps:
      - run:
          command: |
            git config --global user.name "$GIT_USER_NAME"
            git config --global user.email "$GIT_USER_EMAIL"
            git config --global hub.protocol "https"
            mkdir -p ~/.config
            printf "github.com:\n- user: $GITHUB_LOGIN\n  oauth_token: $GITHUB_TOKEN\n  protocol: https\n" > ~/.config/hub
            printf "machine github.com\nlogin $GITHUB_LOGIN\npassword $GITHUB_TOKEN\n" > ~/.netrc
            brew install hub
            pip install codespell
            npm install -g autofix
            git clone https://github.com/gitpod-io/workspace-images
            cd workspace-images
            hub fork
            autofix --branches --branch-suffix=$CIRCLE_BUILD_NUM --push=$GITHUB_LOGIN --pull-request
            cd ..
            git clone https://github.com/eclipse-theia/theia
            cd theia
            hub fork
            autofix --branches --branch-suffix=$CIRCLE_BUILD_NUM --signoff --push=$GITHUB_LOGIN --pull-request
            cd ..

workflows:
  version: 2
  nightly:
    triggers:
      - schedule:
          cron: "5 3 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - autofix-workspace-images
